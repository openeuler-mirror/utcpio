[package]
name = "gnu"
version = "1.0.0"
edition = "2021"
license = "GPL-3.0-or-later"

[lib]
name = "gnu"
path = "lib.rs"
crate-type = ["rlib"]  # 声明为静态库

[dependencies]
lazy_static = "1.5"
libc = "0.2"
nix = { version = "0.29", features = ["fs"] } 
bitflags = "1.3"


